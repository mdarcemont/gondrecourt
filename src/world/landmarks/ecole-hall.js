/** The long low school building with the grey metal barrel roof (restyle only; see ecole.js). */
import * as THREE from 'three';

export const style = { wall: 0xf2f1ec, windows: false, roof: 0x8a9098, roofShape: 'hip' };
export const build = () => new THREE.Group();
